---
import { footerMenu, legalMenu } from '@data/menu';
import Button from '@components/ui/Button.astro';
import { siteConfig } from '@data/config';

interface ButtonProps {
  text: string;
  link: string;
  target?: string;
}

interface Props {
  footerCta?: {
    title: string;
    description: string;
    hideCta: boolean;
    button: ButtonProps;
  };
}

const { footerCta } = Astro.props;

const defaultCta = {
  title: "Contacto con la administración de fincas",
  description: "Ponemos a tu disposición un teléfono con centralita para que ninguna llamada se pierda, un formulario de contacto para atender tus consultas y un asistente virtual disponible las 24 horas.",
  hideCta: false,
  button: {
    text: "Contacto",
    link: "/contact",
    target: "_self"
  }
};

const cta = footerCta || defaultCta;
const currentYear = new Date().getFullYear();
---

<footer class={`bg-background-dark text-white pb-small ${cta.hideCta ? 'pt-large' : ''}`}>
  <div class="site-container mx-auto px-4">
    {!cta.hideCta && (
      <div class="text-center border-b border-white/10 mb-12 py-small">
        <span class="text-2xl md:text-3xl font-semibold mb-4 block font-headline" data-aos="fade-up">{cta.title}</span>
        <p class="text-white/80 mb-6 max-w-2xl mx-auto" data-aos="fade-up" data-aos-delay="100">
          {cta.description}
        </p>
        <Button
          href={cta.button.link}
          variant="ghostLight"
          size="lg"
          data-aos="fade-up"
          data-aos-delay="200"
          {...(cta.button.target && { target: cta.button.target })}
        >
          {cta.button.text}
        </Button>
      </div>
    )}

    <div class="grid grid-cols-1 md:grid-cols-4 gap-12 pb-8 border-b border-white/10">
      <div class="col-span-2">
        <h3 class="font-headline text-lg font-semibold mb-4">Administración de Fincas JMF Ortiz</h3>
        <p class="mt-4">
          <a href={`tel:${siteConfig.Socials.Phone}`} class="text-white hover:underline transition-colors">{siteConfig.Socials.Phone}</a>
        </p>
        <p class="mt-4">
          <a href={`mailto:${siteConfig.Socials.Email}`} class="text-white hover:underline transition-colors">{siteConfig.Socials.Email}</a>
        </p>
        <p class="mt-4 text-white">
          C/ Hilados, número 20, Esc Izq Bajo B, 28850 Torrejón de Ardoz. Madrid
        </p>
      </div>

      <div class="col-span-1">
        <h3 class="text-lg font-semibold mb-4">Enlaces Rápidos</h3>
        <nav>
          <ul class="space-y-2">
            {footerMenu.map(item => (
              <li><a href={item.link} class="text-body-dark hover:text-white transition-colors">{item.name}</a></li>
            ))}
          </ul>
        </nav>
      </div>
    </div>

    <div class="mt-8 flex flex-col md:flex-row justify-between items-center">
      <div class="text-body-dark text-sm">
        © {currentYear} {siteConfig.companyName}. Todos los derechos reservados.
      </div>

      <nav class="mt-4 md:mt-0">
        <ul class="flex flex-wrap gap-6">
          {legalMenu.map(item => (
            <li>
              <a
                href={item.link}
                class="text-sm text-body-dark hover:text-white transition-colors"
              >
                {item.name}
              </a>
            </li>
          ))}
        </ul>
      </nav>
    </div>
  </div>
</footer>
